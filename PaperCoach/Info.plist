<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleDisplayName</key>
	<string>Paper Coach</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<!-- The only cryptography is CryptoKit hashing the app's PIN on the device, which
	     is exempt, so no export-compliance question on each upload. -->
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>

	<!-- Purpose strings. Apple requires one for every protected resource, and the
	     App Store Review Guidelines, 5.1.1(ii) Data Collection and Storage
	     (https://developer.apple.com/app-store/review/guidelines/, read 2026-09-13),
	     put it plainly: "Ensure your purpose strings clearly and completely describe
	     your use of the data." Each string below names what is accessed, when, and
	     what happens to it; the key references are
	     https://developer.apple.com/documentation/bundleresources/information-property-list/nscamerausagedescription
	     and .../nsphotolibraryaddusagedescription. Access is requested at the moment
	     the learner asks for it (the capture flow after a finished lesson), never at
	     launch, and the photo-library key is the add-only one because the app writes
	     a photo and never reads the library. -->
	<key>NSCameraUsageDescription</key>
	<string>Paper Coach uses the camera when you choose to photograph a drawing you have finished, so the page can be kept in your sketchbook on this device.</string>
	<key>NSPhotoLibraryAddUsageDescription</key>
	<string>Paper Coach saves a photo of a finished drawing to your photo library only while "Also save to Photos" is on. It never reads your library.</string>

	<key>UIApplicationSceneManifest</key>
	<dict>
		<key>UIApplicationSupportsMultipleScenes</key>
		<false/>
	</dict>
	<key>UIApplicationSupportsIndirectInputEvents</key>
	<true/>
	<key>UILaunchScreen</key>
	<dict/>
	<key>UIRequiresFullScreen</key>
	<true/>
	<!-- The app is portrait. Landscape is listed because one screen uses it — the
	     player, where a wide subject draws larger with the phone on its side
	     (v3.html#pl-landscape). Everywhere else OrientationLockDelegate narrows the
	     mask back to portrait, so only the player ever rotates. iPad lists the same
	     three: the delegate's answer wins over this list once the app is running. -->
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
</dict>
</plist>

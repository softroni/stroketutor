import { PaywallScreen } from "@/components/premium/PaywallScreen";

/** The paywall itself: the same page as `premium`'s index, shared component. */
export default function Paywall() {
  return <PaywallScreen />;
}

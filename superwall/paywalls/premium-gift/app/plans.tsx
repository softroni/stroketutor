import { PlansScreen } from "@/components/premium/PlansScreen";

/** Rises over the paywall like the app's plans sheet (theme.css, "sheet"). */
export const transition = "sheet";

export default function Plans() {
  return <PlansScreen />;
}
